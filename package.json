{
  "name": "onboarding-automation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pure logic modules for the onboarding automation, mirrored into n8n Code nodes.",
  "scripts": {
    "test": "node --test",
    "demo": "node scripts/demo.js",
    "build:nodes": "node scripts/build-node-bundles.js",
    "build:intake": "node scripts/build-workflow-intake.js",
    "build:notify": "node scripts/build-workflow-notify.js",
    "build:complete": "node scripts/build-workflow-complete.js",
    "build:escalate": "node scripts/build-workflow-escalate.js"
  }
}
